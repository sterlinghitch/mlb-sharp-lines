name: Daily MLB Lines Update

on:
  schedule:
    - cron: '0 16 * * *'   # 12:00 PM ET (EDT = UTC-4, so 16:00 UTC)
    - cron: '0 20 * * *'   # 4:00 PM ET  (EDT = UTC-4, so 20:00 UTC)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-site:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run generator
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: python generate.py

      - name: Commit and push
        run: |
          git config user.name "MLB Bot"
          git config user.email "bot@mlbsharplines.com"
          git add index.html picks.json noon_analysis.json picks_history.json || true
          git diff --staged --quiet || git commit -m "Daily update $(date +'%B %d %Y %H:%M')"
          git pull --rebase origin main
          git push
